---
name: kill-local
description: Stop the local development environment (backend and frontend)
---

Run the shutdown script:

```
bash .claude/skills/kill-local/scripts/kill-local.sh
```

Report which processes were killed and which were not running.
