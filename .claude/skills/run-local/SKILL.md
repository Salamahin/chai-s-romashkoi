---
name: run-local
description: Start the local development environment (backend on :8000, frontend on :5173)
---

Run the startup script:

```
bash .claude/skills/run-local/scripts/run-local.sh
```

Report which servers are up, which (if any) failed, and show the backend response body.
